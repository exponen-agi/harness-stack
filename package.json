{
  "name": "harness-stack",
  "version": "0.1.0",
  "description": "An AI-native, platform-agnostic engineering harness: spec-driven sub-agents you can drop into any project and run on any agentic coding platform.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "harness": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "templates",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "node scripts/prepare.mjs",
    "prepare": "node scripts/prepare.mjs",
    "dev": "tsx src/cli.ts",
    "harness": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src tests eslint.config.js",
    "lint:fix": "eslint src tests eslint.config.js --fix",
    "verify:brain-template": "node scripts/check-brain-template.mjs",
    "eval:agents": "node scripts/eval-agents.mjs"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.3.0",
    "eslint": "^10.9.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.11"
  }
}
